# CLI Module

This module contains the command-line interface for OpenClaw Enterprise Gateway.

## Structure

- `index.ts` - Main CLI entry point
- `commands/` - CLI command implementations
- `utils/` - CLI utility functions

## Commands

- `start` - Start the gateway service
- `stop` - Stop the gateway service
- `status` - Check service status
- `config` - Manage configuration
- `logs` - View logs

## Development

Implementation pending scaffolding approval.
