[1mdiff --git a/scripts/cdms_execute.js b/scripts/cdms_execute.js[m
[1mindex 0e694c9..75da62a 100644[m
[1m--- a/scripts/cdms_execute.js[m
[1m+++ b/scripts/cdms_execute.js[m
[36m@@ -39,6 +39,83 @@[m [mfunction sha256(s) {[m
     return crypto.createHash('sha256').update(s, 'utf8').digest('hex');[m
 }[m
 [m
[32m+[m[32m/**[m
[32m+[m[32m * Normalises a key by removing BOM, trimming whitespace, and lowercasing.[m
[32m+[m[32m */[m
[32m+[m[32mfunction normKey(k) {[m
[32m+[m[32m    if (k == null) return '';[m
[32m+[m[32m    return String(k).replace(/^\ufeff/, '').trim().toLowerCase();[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/**[m
[32m+[m[32m * Robust field getter for CSV rows.[m
[32m+[m[32m * Handles BOM, whitespace, and case issues in headers.[m
[32m+[m[32m * Also strips wrapping quotes from values and trims.[m
[32m+[m[32m */[m
[32m+[m[32mfunction getField(row, name) {[m
[32m+[m[32m    if (!row) return '';[m
[32m+[m[32m    const n = normKey(name);[m
[32m+[m[32m    const keys = Object.keys(row);[m
[32m+[m[32m    const map = {};[m
[32m+[m[32m    keys.forEach(k => { map[normKey(k)] = k; });[m
[32m+[m
[32m+[m[32m    const actualKey = map[n];[m
[32m+[m[32m    let val = actualKey ? row[actualKey] : '';[m
[32m+[m[32m    if (val == null) return '';[m
[32m+[m[32m    val = String(val).trim();[m
[32m+[m[32m    if (val.startsWith('"') && val.endsWith('"')) {[m
[32m+[m[32m        val = val.slice(1, -1).trim();[m
[32m+[m[32m    }[m
[32m+[m[32m    return val;[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/**[m
[32m+[m[32m * Deterministically resolves src/dst paths for a batch row.[m
[32m+[m[32m * Supports Smoke schema (direct paths) and Real schema (joined components).[m
[32m+[m[32m */[m
[32m+[m[32mfunction resolveRowPaths(row, index) {[m
[32m+[m[32m    const g = (k) => getField(row, k);[m
[32m+[m
[32m+[m[32m    // Schema component lookup[m
[32m+[m[32m    const cPath = g('CurrentPath');[m
[32m+[m[32m    const cName = g('CurrentName');[m
[32m+[m[32m    const pTSI = g('ProposedTSIPath') || g('ProposedTSI01Path');[m
[32m+[m[32m    const pName = g('ProposedName');[m
[32m+[m[32m    const sDrive = g('SourceDrive');[m
[32m+[m
[32m+[m[32m    // Detect schema components[m
[32m+[m[32m    const isRealBatch = (sDrive !== '' || cName !== '' || pName !== '');[m
[32m+[m
[32m+[m[32m    if (isRealBatch || cPath) {[m
[32m+[m[32m        if (cName && pName && cPath && pTSI) {[m
[32m+[m[32m            let base = sDrive;[m
[32m+[m[32m            if (!base) {[m
[32m+[m[32m                // Derive base from CurrentPath by taking everything up to and including:[m
[32m+[m[32m                // "TSI - Corporate\" or "TSI – Corporate\" (handle hyphen and en-dash, and / or \ separators)[m
[32m+[m[32m                const match = cPath.match(/(.*?[\\\/]TSI\s+[-–]\s+Corporate[\\\/])/i);[m
[32m+[m[32m                if (match) {[m
[32m+[m[32m                    base = match[1];[m
[32m+[m[32m                } else {[m
[32m+[m[32m                    throw new Error(`Row ${index} cannot derive base drive from CurrentPath`);[m
[32m+[m[32m                }[m
[32m+[m[32m            }[m
[32m+[m
[32m+[m[32m            return {[m
[32m+[m[32m                srcAbs: path.join(cPath, cName),[m
[32m+[m[32m                base: base,[m
[32m+[m[32m                dstAbs: path.join(base, pTSI, pName)[m
[32m+[m[32m            };[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    // Smoke Schema fallback[m
[32m+[m[32m    if (!cPath || !pTSI) {[m
[32m+[m[32m        throw new Error(`Row ${index} missing required fields for path resolution`);[m
[32m+[m[32m    }[m
[32m+[m[32m    // For smoke, we treat them as ready-to-use paths[m
[32m+[m[32m    return { srcAbs: cPath, dstAbs: pTSI };[m
[32m+[m[32m}[m
[32m+[m
 function parseArgs(argv) {[m
     const out = {};[m
     for (let i = 0; i < argv.length; i++) {[m
[36m@@ -101,12 +178,13 @@[m [mfunction fail(msg, code = 1) {[m
 [m
 function main() {[m
     const args = parseArgs(process.argv.slice(2));[m
[32m+[m[32m    const runMode = args.execute ? 'EXECUTE' : 'DRYRUN';[m
 [m
[31m-    // ── --live short-circuit ──────────────────────────────────────────────────[m
[32m+[m[32m    // ── --live short-circuit (legacy) ──────────────────────────────────────────[m
     if (args.live) {[m
         console.error('LIVE_NOT_IMPLEMENTED');[m
         console.error('Live Drive execution is not yet built.');[m
[31m-        console.error('Re-run without --live to produce a dry-run receipt and run sheet.');[m
[32m+[m[32m        console.error('Re-run with --execute for the new implementation.');[m
         process.exit(2);[m
     }[m
 [m
[36m@@ -117,16 +195,32 @@[m [mfunction main() {[m
     const owner = args.owner;[m
 [m
     // ── Load policy ───────────────────────────────────────────────────────────[m
[31m-    console.log('\n=== CDMS Execute — DRY-RUN ===');[m
[32m+[m[32m    console.log(`\n=== CDMS Execute — ${runMode} ===`);[m
[32m+[m[32m    if (runMode === 'EXECUTE') {[m
[32m+[m[32m        console.log('RUN MODE: EXECUTE');[m
[32m+[m[32m    } else {[m
[32m+[m[32m        console.log('RUN MODE: DRYRUN');[m
[32m+[m[32m    }[m
     let policy;[m
     try {[m
         policy = JSON.parse(fs.readFileSync(POLICY_PATH, 'utf8'));[m
     } catch (err) {[m
         fail('Cannot load policy: ' + err.message);[m
     }[m
[31m-    const allowlistRoot = policy.allowlistRoots[0];[m
[32m+[m[32m    const rawRoots = [[m
[32m+[m[32m        ...(policy.allowListRoots || []),[m
[32m+[m[32m        ...(policy.allowlistRoots || []),[m
[32m+[m[32m        policy.allowListRoot[m
[32m+[m[32m    ].filter(Boolean);[m
[32m+[m
[32m+[m[32m    const allowRoots = rawRoots.map(r => {[m
[32m+[m[32m        let n = r.replace(/\\/g, '/');[m
[32m+[m[32m        if (!n.endsWith('/')) n += '/';[m
[32m+[m[32m        return n;[m
[32m+[m[32m    });[m
[32m+[m
     const maxBatchSize = policy.maxBatchSize;[m
[31m-    console.log(`Policy loaded: allowlistRoot="${allowlistRoot}", maxBatchSize=${maxBatchSize}`);[m
[32m+[m[32m    console.log(`Policy loaded: allowlistRoots=[${allowRoots.join(', ')}], maxBatchSize=${maxBatchSize}`);[m
 [m
     // ── Gate 1: batch exists ──────────────────────────────────────────────────[m
     const batchPath = resolveBatchPath(args.batch);[m
[36m@@ -157,18 +251,41 @@[m [mfunction main() {[m
     console.log(`[GATE 2] Row count       : PASS  (${rows.length} rows, max=${maxBatchSize})`);[m
 [m
     // ── Gate 3: allowlist check on every row ──────────────────────────────────[m
[31m-    // Normalise both sides to forward-slash before comparing (MoveMap uses backslash)[m
[31m-    const normRoot = allowlistRoot.replace(/\\/g, '/');[m
[31m-    const violators = rows.filter(r => {[m
[31m-        const normPath = (r.ProposedTSI01Path || '').replace(/\\/g, '/');[m
[31m-        return !normPath.startsWith(normRoot);[m
[32m+[m[32m    // Validate BOTH resolved src and dst are inside ANY allowRoot[m
[32m+[m[32m    const violators = rows.filter((r, i) => {[m
[32m+[m[32m        try {[m
[32m+[m[32m            const res = resolveRowPaths(r, i + 1);[m
[32m+[m[32m            const { srcAbs, dstAbs } = res;[m
[32m+[m
[32m+[m[32m            const n = (p) => p.replace(/\\/g, '/').replace(/\/+/g, '/');[m
[32m+[m[32m            const normSrc = n(srcAbs);[m
[32m+[m[32m            const normDst = n(dstAbs);[m
[32m+[m
[32m+[m[32m            const srcOk = allowRoots.some(root => normSrc.startsWith(n(root)));[m
[32m+[m[32m            const dstOk = allowRoots.some(root => normDst.startsWith(n(root)));[m
[32m+[m
[32m+[m[32m            // Log resolution for Row 1 in DRYRUN[m
[32m+[m[32m            if (i === 0 && runMode === 'DRYRUN') {[m
[32m+[m[32m                console.log(`Row 1 Resolution Check:`);[m
[32m+[m[32m                console.log(`  DEBUG_KEYS: ${Object.keys(r).join(', ')}`);[m
[32m+[m[32m                console.log(`  DEBUG SourceDrive: "${getField(r, 'SourceDrive')}"`);[m
[32m+[m[32m                console.log(`  DEBUG srcAbs: ${srcAbs}`);[m
[32m+[m[32m                console.log(`  DEBUG dstAbs: ${dstAbs}`);[m
[32m+[m[32m                console.log(`  Inside Allowlist: SRC=${srcOk}, DST=${dstOk}`);[m
[32m+[m[32m            }[m
[32m+[m
[32m+[m[32m            return !srcOk || !dstOk;[m
[32m+[m[32m        } catch (err) {[m
[32m+[m[32m            console.error(`  ${err.message}`);[m
[32m+[m[32m            return true;[m
[32m+[m[32m        }[m
     });[m
     if (violators.length > 0) {[m
[31m-        console.error('\nAllowlist violations:');[m
[31m-        violators.forEach(r => console.error(`  "${r.ProposedTSI01Path}"`));[m
[32m+[m[32m        console.error('\nAllowlist violations (src or dst outside allowlistRoots):');[m
[32m+[m[32m        violators.forEach(r => console.error(`  SRC: "${r.CurrentPath}" | DST: "${r.ProposedTSI01Path}"`));[m
         fail(`${violators.length} row(s) violate allowlistRoots. Abort.`);[m
     }[m
[31m-    console.log(`[GATE 3] Allowlist       : PASS  (all ${rows.length} rows → "${allowlistRoot}...")`);[m
[32m+[m[32m    console.log(`[GATE 3] Allowlist       : PASS  (all ${rows.length} rows inside allowlistRoots)`);[m
 [m
     // ── Gate 4: ledger approval ───────────────────────────────────────────────[m
     let db;[m
[36m@@ -197,41 +314,108 @@[m [mfunction main() {[m
         );[m
     }[m
     console.log(`[GATE 4] Ledger approval : PASS  (approved_by="${approval.approved_by}" at ${approval.ts})`);[m
[31m-    console.log('\nGATES_PASSED — all 4 gates passed. Mode: DRY-RUN');[m
[32m+[m[32m    console.log(`\nGATES_PASSED — all 4 gates passed. Mode: ${runMode}`);[m
[32m+[m
[32m+[m[32m    // ── Execute Loop (if mode is EXECUTE) ─────────────────────────────────────[m
[32m+[m[32m    const rowResults = [];[m
[32m+[m[32m    let stopLossTriggered = false;[m
[32m+[m
[32m+[m[32m    if (runMode === 'EXECUTE') {[m
[32m+[m[32m        console.log('\n=== EXECUTING OPERATIONS ===');[m
[32m+[m[32m        for (let i = 0; i < rows.length; i++) {[m
[32m+[m[32m            const row = rows[i];[m
[32m+[m[32m            const label = `[Row ${i + 1}/${rows.length}]`;[m
[32m+[m
[32m+[m[32m            let src, dst;[m
[32m+[m[32m            try {[m
[32m+[m[32m                const res = resolveRowPaths(row, i + 1);[m
[32m+[m[32m                src = res.srcAbs;[m
[32m+[m[32m                dst = res.dstAbs;[m
[32m+[m[32m                console.log(`${label} Validating: "${src}" -> "${dst}"`);[m
[32m+[m
[32m+[m[32m                // 1. Basic Path Validation[m
[32m+[m[32m                if (!src || !dst) throw new Error('Source or Destination path is empty');[m
[32m+[m[32m                if (src.includes('..') || dst.includes('..')) throw new Error('Path traversal (..) detected');[m
[32m+[m
[32m+[m[32m                // 2. Windows Path Length Guard[m
[32m+[m[32m                if (src.length >= 250 || dst.length >= 250) {[m
[32m+[m[32m                    throw new Error(`Path too long (${Math.max(src.length, dst.length)} chars, max 250)`);[m
[32m+[m[32m                }[m
[32m+[m
[32m+[m[32m                // 3. Illegal Character Check (Basic)[m
[32m+[m[32m                const illegal = /[<>:"|?*]/;[m
[32m+[m[32m                // We exclude / and \ because they are path separators[m
[32m+[m[32m                if (illegal.test(path.basename(src)) || illegal.test(path.basename(dst))) {[m
[32m+[m[32m                    throw new Error('Illegal characters detected in filename');[m
[32m+[m[32m                }[m
[32m+[m
[32m+[m[32m                // 4. Filesystem State Validation[m
[32m+[m[32m                if (!fs.existsSync(src)) throw new Error('Source file does not exist');[m
[32m+[m[32m                if (fs.existsSync(dst)) throw new Error('Destination already exists (no overwrite)');[m
[32m+[m[32m                if (!fs.existsSync(path.dirname(dst))) throw new Error(`Target parent directory does not exist: ${path.dirname(dst)}`);[m
[32m+[m
[32m+[m[32m                // 5. Allowlist Re-check (redundant but safe)[m
[32m+[m[32m                const normSrc = src.replace(/\\/g, '/');[m
[32m+[m[32m                const normDst = dst.replace(/\\/g, '/');[m
[32m+[m[32m                if (!allowRoots.some(root => normSrc.startsWith(root))) throw new Error('Source path violates allowlistRoots');[m
[32m+[m[32m                if (!allowRoots.some(root => normDst.startsWith(root))) throw new Error('Target path violates allowlistRoots');[m
[32m+[m
[32m+[m[32m                // 6. Execute Move[m
[32m+[m[32m                fs.renameSync(src, dst);[m
[32m+[m[32m                console.log(`  SUCCESS: Moved.`);[m
[32m+[m[32m                rowResults.push({ index: i, ok: true, src, dst });[m
[32m+[m
[32m+[m[32m            } catch (err) {[m
[32m+[m[32m                console.error(`  FAILURE: ${err.message}`);[m
[32m+[m[32m                rowResults.push({ index: i, ok: false, src: src || 'unknown', dst: dst || 'unknown', error: err.message });[m
[32m+[m
[32m+[m[32m                // CROSS-DEVICE CHECK[m
[32m+[m[32m                if (err && err.code === 'EXDEV') {[m
[32m+[m[32m                    console.error('  CRITICAL: Cross-device move attempted. CDMS policy forbids copy+delete fallback.');[m
[32m+[m[32m                }[m
[32m+[m
[32m+[m[32m                console.error('\nSTOP-LOSS: Aborting remaining operations.');[m
[32m+[m[32m                stopLossTriggered = true;[m
[32m+[m[32m                break;[m
[32m+[m[32m            }[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
 [m
     // ── Write receipt ─────────────────────────────────────────────────────────[m
     if (!fs.existsSync(RECEIPT_DIR)) fs.mkdirSync(RECEIPT_DIR, { recursive: true });[m
 [m
     const ts = nowStamp();[m
[31m-    const receiptPath = path.join(RECEIPT_DIR, `Receipt_${ts}_DRYRUN.json`);[m
[32m+[m[32m    const receiptPath = path.join(RECEIPT_DIR, `Receipt_${ts}_${runMode}.json`);[m
     const receipt = {[m
[31m-        mode: 'DRYRUN',[m
[31m-        batch_ref: batchRef,[m
[31m-        batch_path: batchPath,[m
[32m+[m[32m        mode: runMode,[m
[32m+[m[32m        batch: batchRef,[m
         owner,[m
[31m-        ts: nowIso(),[m
[32m+[m[32m        executed_at: nowIso(),[m
[32m+[m[32m        allowlist_roots: allowRoots,[m
[32m+[m[32m        row_count: rows.length,[m
[32m+[m[32m        stop_loss_triggered: stopLossTriggered,[m
         gates_passed: true,[m
         gates: {[m
             batch_exists: true,[m
[31m-            row_count: rows.length,[m
             max_batch_size: maxBatchSize,[m
             allowlist_ok: true,[m
[31m-            allowlist_root: allowlistRoot,[m
             approval_id: approval.id,[m
             approved_by: approval.approved_by,[m
             approval_ts: approval.ts,[m
         },[m
[31m-        planned_operations: rows.map(r => ({[m
[31m-            source_drive: r.SourceDrive,[m
